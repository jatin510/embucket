query TTT
SELECT COS(0), COS(PI()/3), COS(RADIANS(90))
----
1.0	0.5000000000000001	6.123233995736766e-17

exclude-from-coverage
statement ok
CREATE OR REPLACE TABLE simple (x INTEGER, y INTEGER);

exclude-from-coverage
statement ok
INSERT INTO simple (x, y) VALUES
    (10, 20),
    (20, 44),
    (30, 70);

query T
SELECT COS(x)
    FROM simple
    ORDER BY x
----
-0.8390715290764524
0.40808206181339196
0.15425144988758405